version: "3.8"

services:
  frontend:
    volumes:
      - ./frontend:/web-app
      - /web-app/node_modules # Anonymous volume to prevent overwriting
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
